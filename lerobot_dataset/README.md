# lerobot_dataset

这是一个空文件夹。只有在本地采集数据之后，才会向此处写入数据。

例如运行以下命令后：

```bash
bash RoboSynChallenge/launch/run_task.sh rclick_bell andom 2_1 --headless --max_episodes 1000
```

在本文件夹下会生成一个名为 `click_bell` 的子文件夹，用来存储生成的数据。